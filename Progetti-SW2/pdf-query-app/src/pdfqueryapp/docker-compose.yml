version: '3.8'

services:
  pdf-query-app:
    build: .
    container_name: pdf-query-app
    volumes:
      - ./src/PdfQueryApp/Data:/app/Data:rw
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    environment:
      - DISPLAY=${DISPLAY}
    working_dir: /app
    stdin_open: true
    tty: true